# Скопируйте этот файл в .env и при необходимости настройте URL backend.

# Базовый URL StroyFoto API.
# По умолчанию - same-origin '/api' (production через reverse proxy на тот же домен).
# В dev можно указать http://localhost:4000/api, если backend и vite на разных портах.
VITE_API_URL=/api
